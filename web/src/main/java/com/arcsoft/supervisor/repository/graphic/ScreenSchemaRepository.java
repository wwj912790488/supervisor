package com.arcsoft.supervisor.repository.graphic;

import com.arcsoft.supervisor.model.domain.graphic.ScreenSchema;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Repository interface for {@code ScreenSchema}.
 *
 * @author zw.
 */
public interface ScreenSchemaRepository extends JpaRepository<ScreenSchema, Integer> {
}
