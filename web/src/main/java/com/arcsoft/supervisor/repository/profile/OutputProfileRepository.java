package com.arcsoft.supervisor.repository.profile;

import com.arcsoft.supervisor.model.domain.task.OutputProfile;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * @author zw.
 */
public interface OutputProfileRepository extends JpaRepository<OutputProfile, Integer>{
}
