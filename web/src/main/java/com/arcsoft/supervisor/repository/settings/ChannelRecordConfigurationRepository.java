package com.arcsoft.supervisor.repository.settings;

import com.arcsoft.supervisor.model.domain.system.ChannelRecordConfiguration;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChannelRecordConfigurationRepository extends JpaRepository<ChannelRecordConfiguration, Integer>{
}
